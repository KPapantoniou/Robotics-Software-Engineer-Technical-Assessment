services:
  ros2_dev:
    build : .
    container_name: ros2_container
    environment:
      - DISPLAY=host.docker.internal:0.0
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./src:/ros2_ws/src
    tty: true
